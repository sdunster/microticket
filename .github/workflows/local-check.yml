name: Local stack check
on:
  pull_request:
    paths:
      - 'local/**'
      - 'api/src/bin/local-*.rs'
      - 'api/src/local_dev.rs'
      - 'api/tests/seed_fixtures.rs'
      - 'infra/dynamodb.tf'
      - 'Makefile'
      - '.github/workflows/local-check.yml'
      - '.github/workflows/_check-local.yml'

jobs:
  local_check:
    uses: ./.github/workflows/_check-local.yml
