# Copy to .env.local (gitignored). Nothing is required yet at this stage of the
# build — this file exists so later steps have somewhere obvious to add
# frontend-only config (e.g. VITE_TURNSTILE_DISABLED, a Turnstile site key).
